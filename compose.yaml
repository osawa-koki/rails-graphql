services:
  server:
    build:
      context: ./server/
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    volumes:
      - ./server/:/server/
      - bundle:/usr/local/bundle

volumes:
  bundle:
